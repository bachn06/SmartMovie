import XCTest
@testable import SmartMoviePackage

final class SmartMoviePackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SmartMoviePackage().text, "Hello, World!")
    }
}
